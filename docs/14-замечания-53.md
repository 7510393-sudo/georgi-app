# Замечания по сборке 53 — 23 сентября

Список автора целиком. Отмечается по мере исполнения; собирается всё разом,
по слову «собери» (P151).

| № | Что | Состояние |
|---|---|---|
| 1 | **«Как прошло?»: текст пропадает.** Дважды за день | сделано |
| 2 | «OBJ» в пунктирной рамке в конце абзацев дневника | сделано |
| 3 | Перетаскивание дела: строка под ним не освобождает место | сделано |
| 4 | В режиме изменений убрать стрелки вверх-вниз; крестик вдвое крупнее | сделано |
| 5 | **Корзина**: вернуть удалённое в течение 30 дней. Где живёт и как устроена — предложить варианты | предложено |
| 6 | В плане короткое нажатие тоже затеняет дело — оставить только долгое. Номера дрожат после выхода из режима изменений | сделано |
| 7 | Возврат на «Сегодня» кнопкой — с перелистыванием страниц. То же в календаре | |
| 8 | В списке календаря на дело — ровно одна строка, длинное с троеточием | сделано |
| 9 | Шрифт заголовка дня в дневнике занимает много места | сделано |
| 10 | **Геоточка (впрок):** координаты ставятся туда, где стоит курсор. Карта в планировщике — позже | предложено |
| 11 | На экране поиска свайп вбок перебрасывает на «Сегодня» | сделано |

---

## 1. Почему пропадал текст «Как прошло?»

Причина в сборке ответов обратно в файл. Ответы привязаны к названию дела,
а при записи в файл оставлялись **только те, чьё название есть в сегодняшнем
списке дел**:

```swift
let kept = order.filter { (answers[$0] ?? "").isEmpty == false }
```

Всё остальное молча исчезало. Это случалось, если после написания ответа
название дела изменилось хоть на знак, дело удалили или оно оказалось
пустым и было убрано при уходе со дня.

Вторая причина в разборе файла: строка делилась по **последнему** двоеточию.
Ответ «сделал в 10:30» делился по двоеточию внутри времени, и название дела
превращалось в «Дело: сделал в 10». На следующей записи такого дела в списке
не было — и ответ пропадал уже совсем.

Обе исправлены 23.09: ответы без пары больше не выбрасываются, а разбор
сперва сверяется с настоящими названиями дел.
